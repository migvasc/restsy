<%= form_for(@my_list) do |f| %>
  <% if @my_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_list.errors.count, "error") %> prohibited this my_list from being saved:</h2>

      <ul>
      <% @my_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :query %><br>
    <%= f.text_field :query %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
